{
  "name": "@koota/core",
  "version": "0.0.1",
  "description": "Core vanilla library for Koota",
  "private": true,
  "license": "ISC",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "bench": "labs",
    "test": "NODE_OPTIONS=--expose-gc vitest",
    "lint": "oxlint",
    "check:kernel": "tsc --noEmit -p tsconfig-kernel.json"
  },
  "dependencies": {
    "@koota/collections": "workspace:*"
  },
  "devDependencies": {
    "@pmndrs/labs": "^0.9.0",
    "@config/oxlint": "workspace:*",
    "@config/typescript": "workspace:*",
    "vitest": "catalog:"
  }
}
